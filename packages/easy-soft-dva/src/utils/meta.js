/**
 * Module Package Name
 */
const modulePackageName = 'easy-soft-dva';

/**
 * Build prompt module info
 */
export function buildPromptModuleInfo(moduleName) {
  return `${modulePackageName}::${moduleName}::`;
}
